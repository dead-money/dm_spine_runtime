/target

# Build artefacts from the spine_capture golden-generator harness.
/tools/spine_capture/build/

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~
.*.un~
*.orig

# OS cruft
.DS_Store
Thumbs.db

# Claude Code personal overrides (team settings.json is committed)
.claude/settings.local.json

# Ad-hoc output from diagnostic examples (software_render, debug dumps).
# Run from the repo root, these write next to Cargo.toml.
/*.png
/*.ppm

# Working notes. These are authored per-session and are not intended to
# live in the published tree — keep them local.
/NEXT_SESSION.md
/PHASE_*.md
